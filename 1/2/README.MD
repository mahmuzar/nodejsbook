В Node.js Event Loop — это механизм, который позволяет выполнять асинхронный код в однопоточной среде. Он состоит из нескольких фаз, каждая из которых отвечает за определённый тип задач. Порядок выполнения коллбэков зависит от того, в какую фазу они попадают.
Основные фазы Event Loop в Node.js:
____
# Node.js Event Loop — Схема и Порядок Выполнения

## 🔄 Основные фазы Event Loop (по порядку)

1. **Timers**  
   → Выполняются `setTimeout`, `setInterval`, если истекло время.

2. **Pending I/O callbacks**  
   → Отложенные I/O-коллбэки (редко используется в пользовательском коде).

3. **Idle / Prepare**  
   → Внутреннее использование (игнорируется при разработке).

4. **Poll**  
   → Обработка новых I/O-событий.  
   → Выполняются коллбэки от завершённых асинхронных операций (например, `fs.readFile`).  
   → Если очередь таймеров пуста, может выполнять `setImmediate` здесь.

5. **Check**  
   → Выполняются коллбэки, зарегистрированные через `setImmediate`.

6. **Close callbacks**  
   → Например: `socket.on('close', ...)`

После фазы **Close** цикл возвращается к **Timers** и повторяется.

---

## ⚡ Очереди с более высоким приоритетом (не фазы!)

Эти очереди **обрабатываются до перехода к следующей фазе**:

| Очередь           | Тип               | Когда выполняется? |
|-------------------|-------------------|--------------------|
| `process.nextTick`| nextTick queue    | **Сразу после текущей операции**, до микрозадач и фаз |
| Microtasks        | `Promise.then`, `queueMicrotask` | После `nextTick`, **перед выходом из текущей фазы** |

> 🔔 **Правило**:  
> `process.nextTick` → Микрозадачи → Текущая фаза Event Loop → Следующая фаза

---

## 🧪 Порядок выполнения — Пример

```js
setTimeout(() => console.log('1. setTimeout'), 0);
setImmediate(() => console.log('2. setImmediate'));
process.nextTick(() => console.log('3. nextTick'));
Promise.resolve().then(() => console.log('4. promise'));
```
## Возможный вывод (в корне скрипта):

3. nextTick
4. promise
1. setTimeout
2. setImmediate

## Но внутри I/O-коллбэка (например, fs.readFile):

3. nextTick
4. promise
2. setImmediate
1. setTimeout

> 💡 Причина: в I/O-контексте мы уже после фазы Poll, поэтому следующая фаза — Check (setImmediate), а Timers будет только в следующем цикле.

## 📌 Краткая шпаргалка по приоритету

1. process.nextTick()  
2. Микрозадачи (Promise.then, queueMicrotask)  
3. Timers: setTimeout / setInterval  
4. Poll: I/O коллбэки  
5. Check: setImmediate  
6. Close: закрытие ресурсов

> 🔄 После этого — новый цикл с Timers.

