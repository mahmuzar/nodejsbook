Вот простой пример стрим-процессора на TypeScript, который читает CSV из файла, преобразует его в JSON и записывает результат в другой файл. Для этого используются встроенные модули Node.js (fs, stream)
____

## 🔍 Важные ограничения:

- ❌ Не поддерживает значения с запятыми внутри, например: "Smith, John",30
- ❌ Не поддерживает переносы строк внутри ячеек
- ❌ Не обрабатывает кавычки или escape-символы

## ✅ Пример input.csv (поддерживаемый):

```csv
name,age,city
Alice,30,Moscow
Bob,25,St. Petersburg
```

## 🔽 output.json:

```json
{"name":"Alice","age":"30","city":"Moscow"}
{"name":"Bob","age":"25","city":"St. Petersburg"}
```